<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <form action="/books/add" method="POST">
        <fieldset>
        <legend>New book</legend>
        <div>
            <label for="title" title="">Title:</label> <input type="text" name="title" id="title" size="30">
        </div>
        <div>
            <label for="author">Author:</label>
            <% locals.selectData = authorsSelect; %>s
            <%- include(`partials/_select`); %>
        </div>
        <div>
            <label for="language">Language:</label>
            <% locals.selectData = languagesSelect; %>
            <%- include(`partials/_select`); %>
        </div>
        <div>
            <label for="" publisher="">Publisher:</label> <input type="text" name="publisher" id="publisher"></div>
        <div>
            <label for="" ISBN="">ISBN:</label> <input type="text" name="ISBN" id="ISBN" size="10">
        </div>

        <% if (error) {%>
            <p style="color: red;"><%= error %></p>
        <% } %>

        <hr>
        <div class="submitButtons">
        <input type="submit" value="Submit">
        <input type="reset" value="Reset">
        </div>
        </fieldset>
    </form>
</body>
</html>
